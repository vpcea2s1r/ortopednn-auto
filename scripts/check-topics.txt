cat /data/topics.json | node -e "const t=require('fs').readFileSync('/dev/stdin','utf8'); const d=JSON.parse(t); d.topics.forEach((x,i)=>console.log(i,x.status,x.topic?x.topic.slice(0,60):'(no topic)'))" 2>/dev/null || cat /data/topics.json